# The mod compiles against the game's own assemblies; the dependency surface is
# the SHA-pinned CI actions plus the Python tooling locked in uv.lock.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: uv
    directory: /
    schedule:
      interval: weekly
