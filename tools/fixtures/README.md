# fixtures

Planned home for versioned synthetic traces (Layer 4) and the labeled false-positive
regression corpus (Layer 7). Each trace carries the hook-manifest hash of the build that
recorded it; a mismatch skips and reports. Empty until Phase 4.
