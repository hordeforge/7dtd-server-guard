# tests

Planned test projects. Layer mapping: `ServerGuard.Tests` = TEST_PLAN.md layers 1-2 (unit +
property), `ServerGuard.MetadataTests` = layer 3 (metadata contract against the pinned
build), `ServerGuard.Replay` = layer 4 (offline replay of seeded traces). Fixture traces
live in `tools/fixtures/`. See [docs/TEST_PLAN.md](../docs/TEST_PLAN.md).
