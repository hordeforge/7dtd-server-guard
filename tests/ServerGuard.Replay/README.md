# ServerGuard.Replay

Offline replay harness for versioned synthetic traces; bit-reproducible for a fixed seed;
mutation suite over every validator input; fingerprint/schema mismatch skips and reports.
TEST_PLAN.md layer 4.
