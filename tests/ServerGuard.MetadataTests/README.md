# ServerGuard.MetadataTests

Metadata-contract tests against the locally installed pinned build (V3.1.0 b14): resolve
every type, full signature, parameter role, return type, metadata token; verify fail-open
resolution and the runtime fault guard. TEST_PLAN.md layer 3.
