[mypy]
strict = True
mypy_path = tools
# Extra gates beyond --strict, all proven clean on the current tree:
warn_unreachable = True
enable_error_code =
    ignore-without-code,
    redundant-expr,
    truthy-bool
