# Detection

`InvariantEngine`: Hard predicates with complete authoritative inputs; may reject in
`correct` mode. `BehaviorEngine`: windowed scoring with uncertainty over copied data on a
worker; weak/review-only. `ContextSnapshot`: latency, tick health, buffs, vehicle, admin,
teleport, stance, death, spawn context providers.
